/**
 * EXPERT_CONTEXT — insertion point for proprietary methodology.
 *
 * Replace the topic bodies below with original writing on evaluating startups.
 * The model should reason using this material in its own words and must not
 * quote it directly to the user.
 */
export const EXPERT_CONTEXT = `
[PLACEHOLDER: proprietary methodology content will be inserted here, organized by topic.

## business-model
(empty)

## traction-signals
(empty)

## deal-structure
(empty)

## red-flags
(empty)

Model should reason using this material in its own words, not quote it directly to the user.]
`.trim();
